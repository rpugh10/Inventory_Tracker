Starting this document later

August 30th 2026
.Implemented UserDetails in the AppUser Entity and overrode the necessary methods 
.Created AppUserDetailsService that implements UserDetailsService to allow Spring Security to find the user 
.Created a PasswordEncoder Bean in SecurityConfig and injected it into the AppUserService
.Created a Role Enum to restrict the types of roles users can be 
.Created PasswordRequestDTO and UpdateRole DTO. The PasswordRequestDTO will allow users to update just their password.
The UpdateRole needs to just be for Admins to update user roles
.Created custom exceptions. Created UserNotFound and DuplicateUser and let a GlobalExceptionHandler return the correct HTTP Status
.Added custom queries to the AppUserRepository. Added findByUsername, findByEmail, existsByUsernameAndIdNot and the same for email.
These queries will allow me to check if a username or email already exists in the database
.Updated the createUser and updateUser methods in the AppUserService to hash passwords, check if username 
and emails already exist and throw my custom exceptions. 
.Updated the AppUserController by adding more endpoints to update the password and roles.
WHATS NEXT
.Tomorrow we need to Connect AppUserDetailsService to Spring Security (done)
.Configure my login and Authentication 
.Protect endpoints based on roles.
.Test the AppUserController endpoints once these are all complete.


August 31st 2026
.Created a JWTUtility class
.Created my encoded JWT secret and stored it in secrets.properties
.Used jwt.secret = ${JWT_SECRET} instead of typing the secret in the application.properties
.In my JWTUtility class injected that secret using @Value(("${jwt.secret}")) and storing it in a storing
WHATS NEXT
.Generate a token (Done)
.Set an expiration (Done)
.Add role into the JWT (Done)
.Figure out how to validate the token (Done)
.Extract claims to figure out who this token is for (Done)

September 2nd 2026
.Implemented getUsername(), generateToken(), and validateToken() methods in the JWT Utility class.
WHATS NEXT
.Connect the JWTUtility class to the login/Authentication endpoint.(Done)
.Connect the JWTUtility to every future request(Done)

September 4th 2026
.Created a separate controller for logins
.Created a login DTO that requests only username and password
.Add JwtAuthenticationFilter class that intercepts requests before they reach the controller
.Created a AuthenticationManager bean in the SecurityConfig class and injected it into my AuthenticationController
WHATS NEXT
.Need to figure out how to generate a token from a successful Authentication result rather than from the input request (Done)
.Test the endpoints once this error is dealt with

September 7th 2026
.Created an Update User DTO with the fields username and email so users can update those
.Created the endpoint int AppUser controller
.Removed Delete requiring admin role in Security Config and instead used PreAuthorize in the controller
WHATS NEXT
.Need to figure out how to create the first Admin (Done)
.Need to only allow users to update their own fields and not other users (Done)
.Define what the ADMINS can modify (Done)

September 8th 2026
.Added an AGENTS.md file to ensure that my project is on the right track
.Added Swagger to POM file and allowed it's endpoints to be permitted in the SecurityConfig
.Created a DataInitializer class to create an Admin account if theres not one created already
.Removed the Role USER so now new accounts automatically default to STAFF
.Tested all endpoints from USER and STAFF views 
WHATS NEXT
.Draw an endpoint to role table diagram
.Start milestone 3
